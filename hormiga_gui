import pygame
import hormiga_logica as hor
import easygui

tick = 60

def main():
    pygame.init()
    clock = pygame.time.Clock()

    filas = int(easygui.enterbox("Cantidad de filas"))
    columnas = int(easygui.enterbox("Cantidad de columnas"))
    tam = int(easygui.enterbox("Tamaño de las celdas"))
    regla = easygui.enterbox("Regla de la hormiga (ejemplos: LR, RLR, LLRR)")

    M = hor.generar_matriz_vacia(filas, columnas)
    hormiga = hor.crear_hormiga(filas, columnas)
    colores = hor.generar_colores(len(regla))

    w, h = columnas * tam, filas * tam
    window = pygame.display.set_mode((w, h))

        pygame.display.update()
        clock.tick(tick)

    pygame.quit()


if __name__ == "__main__":
    main()
